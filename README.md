# 1. Установка базовой библиотеки
sudo dpkg -i libfptr10_*.deb

# 2. Установка GUI (интерфейса), если он есть
sudo dpkg -i libfptr10-gui_*.deb

# 3. Установка утилиты тестирования (Тест драйвера ККТ)
sudo dpkg -i fptr10-test-util_*.deb


#4. Исправление зависимостей (важно)
Драйверы АТОЛ могут требовать библиотеки, которых нет в Debian 13 (например, Qt4 или старые версии libssl). Выполните команду для автоматического поиска и установки недостающих зависимостей:
bash
sudo apt-get install -f
